version: "2"

run:
  tests: true

linters:
  default: none
  enable:
    - govet
    - staticcheck
    - errcheck
    - unused
    - ineffassign
  exclusions:
    paths:
      - '.*_templ\.go$'
      - '.*\.pb\.go$'
      - '.*/database/.*\.go$'
