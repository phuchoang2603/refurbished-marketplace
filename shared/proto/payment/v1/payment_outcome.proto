syntax = "proto3";

package payment.v1;

option go_package = "github.com/phuchoang2603/refurbished-marketplace/shared/proto/payment/v1;paymentv1";

// Payload for payment_outbox rows (topics payment.succeeded | payment.failed).
message PaymentOutcome {
  string order_id = 1;
}
