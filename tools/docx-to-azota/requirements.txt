# CPU structural converter (always)
# Python 3.10+

# Optional local rasterization of WMF/EMF formula previews
# pillow>=10.0.0
# Wand (ImageMagick) or LibreOffice are used if present.

# GPU overlays — install on Colab via install_colab.py (NOT unimernet[full])
