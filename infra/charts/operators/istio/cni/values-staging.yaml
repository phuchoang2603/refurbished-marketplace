# Staging (RKE2) overlays on values.yaml — default CNI paths, not k3s.
cni:
  profile: ambient
  global:
    platform: ""
  cniBinDir: /opt/cni/bin
  cniConfDir: /etc/cni/net.d
  resources:
    requests:
      cpu: 50m
      memory: 100Mi
    limits:
      cpu: 500m
      memory: 256Mi
