# Prod overlay (Argo `prod-root` valueFiles). Doppler env is the cluster
# `operators/doppler-token` Secret, not this file.
ingress:
  webHostname: shop.phuchoang.sbs
  simulatorHostname: pay.phuchoang.sbs
